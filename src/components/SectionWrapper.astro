---
interface Props {
  id: string;
  title?: string;
}

const {id, title} = Astro.props;
---

<section
  id={id}
  class="section"
>
  {title && <h2>{title}</h2>}
  <slot />
</section>


<style>
  .section:has(article) {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
</style>